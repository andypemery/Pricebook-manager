export const dynamic="force-dynamic"; import { notFound } from "next/navigation"; import { prisma } from "@/lib/prisma"; import { requireUser } from "@/lib/auth"; import { updateSampleRecord } from "@/lib/actions/sample-record.actions"; import { SubmitButton } from "@/components/submit-button";
export default async function EditRecord({params}:{params:Promise<{id:string}>}){ const {id}=await params; const user=await requireUser(); const record=await prisma.sampleRecord.findFirst({where:{id,tenantId:user.tenantId}}); if(!record) notFound(); const action=updateSampleRecord.bind(null,id); return <section className="card"><h1>Edit record</h1><form action={action as unknown as (formData: FormData) => void}><label className="field"><span>Title</span><input name="title" defaultValue={record.title} required/></label><label className="field"><span>Status</span><input name="status" defaultValue={record.status}/></label><label className="field"><span>Description</span><textarea name="description" defaultValue={record.description||""}/></label><SubmitButton>Save changes</SubmitButton></form></section> }
