export const dynamic="force-dynamic"; import Link from "next/link"; import { notFound } from "next/navigation"; import { prisma } from "@/lib/prisma"; import { requireUser } from "@/lib/auth"; import { archiveSampleRecord } from "@/lib/actions/sample-record.actions";
export default async function RecordDetail({params}:{params:Promise<{id:string}>}){const {id}=await params; const user=await requireUser(); const record=await prisma.sampleRecord.findFirst({where:{id,tenantId:user.tenantId},include:{notes:{orderBy:{createdAt:"desc"}}}}); if(!record) notFound(); const archive=archiveSampleRecord.bind(null,id); return <section className="card"><h1>{record.title}</h1><p>Status: {record.status}</p><p>{record.description}</p><div className="actions"><Link className="primary" href={`/sample-records/${id}/edit`}>Edit</Link><form action={archive as unknown as (formData: FormData) => void}><button className="secondary">Archive</button></form></div></section>}
