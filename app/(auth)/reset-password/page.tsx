export const dynamic="force-dynamic"; import { resetPasswordAction } from "@/lib/actions/auth.actions"; import { PasswordField } from "@/components/password-field"; import { SubmitButton } from "@/components/submit-button";
export default async function Reset({searchParams}:{searchParams:Promise<{token?:string}>}){const p=await searchParams;return <div className="loginWrap"><form className="loginCard" action={resetPasswordAction as unknown as (formData: FormData) => void}><h1>Choose new password</h1><input type="hidden" name="token" value={p.token||""}/><PasswordField name="password" label="New password"/><PasswordField name="confirmPassword" label="Confirm new password"/><SubmitButton>Reset password</SubmitButton></form></div>}
